apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: home-server-issuer
  namespace: cert-manager
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: mark.yehia@gmail.com
    privateKeySecretRef:
      name: home-server-issuer
    solvers:
      - http01:
          ingress:
            class: nginx
